/**
 * See CipherBreaker.txt for instructions.
 * TODO: add your netid to the line below
 * 
 * @author pescar2
 */
public class CipherBreaker
{
	public static void main(String[] args)
	{
		TextIO.putln("Text?");
		String line = TextIO.getln();
		TextIO.putln(line);
		
		if (line.toUpperCase().indexOf('A') >= 0)
		{
			int counter = 0;
			int j;
			for (j= 0; j < line.length(); j++)
			{
				if(line.toUpperCase().charAt(line.toUpperCase().indexOf('A')) == line.toUpperCase().charAt(j))
				{
					counter++;		
				}
			}
			TextIO.putln("A:" + counter);
		}
		if (line.toUpperCase().indexOf('B') >= 0)
		{
			int counter = 0;
			int j;
			for (j= 0; j < line.length(); j++)
			{
				if (line.toUpperCase().charAt(line.toUpperCase().indexOf('B')) == line.toUpperCase().charAt(j))
				{
					counter++;
				}
			}
			TextIO.putln("B:" + counter);
		}
		if (line.toUpperCase().indexOf('C') >= 0)
		{
			int counter = 0;
			int j;
			for (j= 0; j < line.length(); j++)
			{
				if (line.toUpperCase().charAt(line.toUpperCase().indexOf('C')) == line.toUpperCase().charAt(j))
				{
					counter++;
				}
			}
			TextIO.putln("C:" + counter);
		}
		if (line.toUpperCase().indexOf('D') >= 0)
		{
			int counter = 0;
			int j;
			for (j= 0; j < line.length(); j++)
			{
				if (line.toUpperCase().charAt(line.toUpperCase().indexOf('D')) == line.toUpperCase().charAt(j))
				{
					counter++;
				}
			}
			TextIO.putln("D:" + counter);
		}
		if (line.toUpperCase().indexOf('E') >= 0)
		{
			int counter = 0;
			int j;
			for (j= 0; j < line.length(); j++)
			{
				if (line.toUpperCase().charAt(line.toUpperCase().indexOf('E')) == line.toUpperCase().charAt(j))
				{
					counter++;
				}
			}
			TextIO.putln("E:" + counter);
		}
		if (line.toUpperCase().indexOf('F') >= 0)
		{
			int counter = 0;
			int j;
			for (j= 0; j < line.length(); j++)
			{
				if (line.toUpperCase().charAt(line.toUpperCase().indexOf('F')) == line.toUpperCase().charAt(j))
				{
					counter++;
				}
			}
			TextIO.putln("F:" + counter);
		}
		if (line.toUpperCase().indexOf('G') >= 0)
		{
			int counter = 0;
			int j;
			for (j= 0; j < line.length(); j++)
			{
				if (line.toUpperCase().charAt(line.toUpperCase().indexOf('G')) == line.toUpperCase().charAt(j))
				{
					counter++;
				}
			}
			TextIO.putln("G:"+ counter);
		}
		if (line.toUpperCase().indexOf('H') >= 0)
		{
			int counter = 0;
			int j;
			for (j= 0; j < line.length(); j++)
			{
				if (line.toUpperCase().charAt(line.toUpperCase().indexOf('H')) == line.toUpperCase().charAt(j))
				{
					counter++;
				}
			}
			TextIO.putln("H:" + counter);
		}
		if (line.toUpperCase().indexOf('I') >= 0)
		{
			int counter = 0;
			int j;
			for (j= 0; j < line.length(); j++)
			{
				if (line.toUpperCase().charAt(line.toUpperCase().indexOf('I')) == line.toUpperCase().charAt(j))
				{
					counter++;
				}
			}
			TextIO.putln("I:" + counter);
		}
		if (line.toUpperCase().indexOf('J') >= 0)
		{
			int counter = 0;
			int j;
			for (j= 0; j < line.length(); j++)
			{
				if (line.toUpperCase().charAt(line.toUpperCase().indexOf('J')) == line.toUpperCase().charAt(j))
				{
					counter++;
				}
			}
			TextIO.putln("J:" + counter);
		}
		if (line.toUpperCase().indexOf('K') >= 0)
		{
			int counter = 0;
			int j;
			for (j= 0; j < line.length(); j++)
			{
				if (line.toUpperCase().charAt(line.toUpperCase().indexOf('K')) == line.toUpperCase().charAt(j))
				{
					counter++;
				}
			}
			TextIO.putln("K:" + counter);
		}
		if (line.toUpperCase().indexOf('L') >= 0)
		{
			int counter = 0;
			int j;
			for (j= 0; j < line.length(); j++)
			{
				if (line.toUpperCase().charAt(line.toUpperCase().indexOf('L')) == line.toUpperCase().charAt(j))
				{
					counter++;
				}
			}
			TextIO.putln("L:" + counter);
		}
		if (line.toUpperCase().indexOf('M') >= 0)
		{
			int counter = 0;
			int j;
			for (j= 0; j < line.length(); j++)
			{
				if (line.toUpperCase().charAt(line.toUpperCase().indexOf('M')) == line.toUpperCase().charAt(j))
				{
					counter++;
				}
			}
			TextIO.putln("M:" + counter);
		}
		if (line.toUpperCase().indexOf('N') >= 0)
		{
			int counter = 0;
			int j;
			for (j= 0; j < line.length(); j++)
			{
				if (line.toUpperCase().charAt(line.toUpperCase().indexOf('N')) == line.toUpperCase().charAt(j))
				{
					counter++;
				}
			}
			TextIO.putln("N:" + counter);
		}
		if (line.toUpperCase().indexOf('O') >= 0)
		{
			int counter = 0;
			int j;
			for (j= 0; j < line.length(); j++)
			{
				if (line.toUpperCase().charAt(line.toUpperCase().indexOf('O')) == line.toUpperCase().charAt(j))
				{
					counter++;
				}
			}
			TextIO.putln("O:" + counter);
		}
		if (line.toUpperCase().indexOf('P') >= 0)
		{
			int counter = 0;
			int j;
			for (j= 0; j < line.length(); j++)
			{
				if (line.toUpperCase().charAt(line.toUpperCase().indexOf('P')) == line.toUpperCase().charAt(j))
				{
					counter++;
				}
			}
			TextIO.putln("P:" + counter);
		}
		if (line.toUpperCase().indexOf('Q') >= 0)
		{
			int counter = 0;
			int j;
			for (j= 0; j < line.length(); j++)
			{
				if (line.toUpperCase().charAt(line.toUpperCase().indexOf('Q')) == line.toUpperCase().charAt(j))
				{
					counter++;
				}
			}
			TextIO.putln("Q:" + counter);
		}
		if (line.toUpperCase().indexOf('R') >= 0)
		{
			int counter = 0;
			int j;
			for (j= 0; j < line.length(); j++)
			{
				if (line.toUpperCase().charAt(line.toUpperCase().indexOf('R')) == line.toUpperCase().charAt(j))
				{
					counter++;
				}
			}
			TextIO.putln("R:" + counter);
		}
		if (line.toUpperCase().indexOf('S') >= 0)
		{
			int counter = 0;
			int j;
			for (j= 0; j < line.length(); j++)
			{
				if (line.toUpperCase().charAt(line.toUpperCase().indexOf('S')) == line.toUpperCase().charAt(j))
				{
					counter++;
				}
			}
			TextIO.putln("S:" + counter);
		}
		if (line.toUpperCase().indexOf('T') >= 0)
		{
			int counter = 0;
			int j;
			for (j= 0; j < line.length(); j++)
			{
				if (line.toUpperCase().charAt(line.toUpperCase().indexOf('T')) == line.toUpperCase().charAt(j))
				{
					counter++;
				}
			}
			TextIO.putln("T:" + counter);
		}
		if (line.toUpperCase().indexOf('U') >= 0)
		{
			int counter = 0;
			int j;
			for (j= 0; j < line.length(); j++)
			{
				if (line.toUpperCase().charAt(line.toUpperCase().indexOf('U')) == line.toUpperCase().charAt(j))
				{
					counter++;
				}
			}
			TextIO.putln("U:" + counter);
		}
		if (line.toUpperCase().indexOf('V') >= 0)
		{
			int counter = 0;
			int j;
			for (j= 0; j < line.length(); j++)
			{
				if (line.toUpperCase().charAt(line.toUpperCase().indexOf('V')) == line.toUpperCase().charAt(j))
				{
					counter++;
				}
			}
			TextIO.putln("V:" + counter);
		}
		if (line.toUpperCase().indexOf('W') >= 0)
		{
			int counter = 0;
			int j;
			for (j= 0; j < line.length(); j++)
			{
				if (line.toUpperCase().charAt(line.toUpperCase().indexOf('W')) == line.toUpperCase().charAt(j))
				{
					counter++;
				}
			}
			TextIO.putln("W:" + counter);
		}
		if (line.toUpperCase().indexOf('X') >= 0)
		{
			int counter = 0;
			int j;
			for (j= 0; j < line.length(); j++)
			{
				if (line.toUpperCase().charAt(line.toUpperCase().indexOf('X')) == line.toUpperCase().charAt(j))
				{
					counter++;
				}
			}
			TextIO.putln("X:" + counter);
		}
		if (line.toUpperCase().indexOf('Y') >= 0)
		{
			int counter = 0;
			int j;
			for (j= 0; j < line.length(); j++)
			{
				if (line.toUpperCase().charAt(line.toUpperCase().indexOf('Y')) == line.toUpperCase().charAt(j))
				{
					counter++;
				}
			}
			TextIO.putln("Y:" + counter);
		}
		if (line.toUpperCase().indexOf('Z') >= 0)
		{
			int counter = 0;
			int j;
			for (j= 0; j < line.length(); j++)
			{
				if (line.toUpperCase().charAt(line.toUpperCase().indexOf('Z')) == line.toUpperCase().charAt(j))
				{
					counter++;
				}
			}
			TextIO.putln("Z:" + counter);
		}

		int digitcounter = 0;
		int spacecounter = 0;
		int punctuationcounter = 0;
		int k;
		for (k = 0; k < line.length(); k++)
		{
			if (line.charAt(k) == '0' || line.charAt(k) == '1' || line.charAt(k) == '2' || line.charAt(k) == '3' || line.charAt(k) == '4' || line.charAt(k) == '5' || line.charAt(k) == '6' || line.charAt(k) == '7' || line.charAt(k) == '8' || line.charAt(k) == '9')
			{
				digitcounter ++; 
			} 
			else if (line.charAt(k) == ' ')
			{
				spacecounter ++;
			}
			else if (line.charAt(k) == '"' || line.charAt(k) == '+' || line.charAt(k) == '-' || line.charAt(k) == '\'' || line.charAt(k) == '!' || line.charAt(k) == '.' || line.charAt(k) == ',' )
			{
				punctuationcounter ++;
			}
		}
		
		if (digitcounter > 0)
		{
			TextIO.putln("DIGITS:" + digitcounter);
		}
		if (spacecounter > 0)
		{
			TextIO.putln("SPACES:" + spacecounter);
		}
		if (punctuationcounter > 0)
		{
			TextIO.putln("PUNCTUATION:" + punctuationcounter);
		}
		
	}//end of main	
}//end of class
